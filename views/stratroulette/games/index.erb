<h2>alla spel:</h2>
<ul>
  <% @games.each do |game| %>
    <li>
      <a href="/games/<%= game['id'] %>" style="text-decoration: none; color: black">
        <%= game['name'] %>
      </a>
      <p>
      <%= game['description'] %>
      </p>
    </li>
  <% end %>
</ul>
